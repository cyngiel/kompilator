program example(input, output);
var x, y: integer;
var g,h:real;

function gcd(a, b: integer):integer;
var g,i:integer;
begin
   if (1 < 2)then
    g:=0.0
  else
    g:=1.0
end;


begin
  x:=1;
  y:=2;
  write(gcd(x, y))
end.
